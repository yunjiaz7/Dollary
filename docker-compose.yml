services:
  db:
    image: postgres:16
    container_name: billingbook-db
    environment:
      POSTGRES_DB: billingbook
      POSTGRES_USER: billingbook
      POSTGRES_PASSWORD: billingbook
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
